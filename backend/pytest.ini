[pytest]
testpaths = tests
pythonpath = .
addopts = -m "not integration"
markers =
    integration: live GigaChat calls (requires GIGACHAT_AUTH_KEY; day 17+)
    live: alias for integration — manual / workflow_dispatch only
filterwarnings =
    ignore::DeprecationWarning
